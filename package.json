{
  "name": "side-project-boilerplate",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "dev:studio": "turbo dev --filter=@apps/studio"
  },
  "devDependencies": {
    "@better-auth/cli": "^1.2.8",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "prettier": "catalog:",
    "turbo": "latest"
  },
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@remixicon/react": "catalog:",
    "@tanstack/react-table": "catalog:",
    "better-auth": "catalog:",
    "eslint": "catalog:"
  }
}
